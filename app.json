{
  "environments": {
    "review": {
      "addons": ["heroku-postgresql:hobby-dev"],
      "env": {
        "HEROKU_API_KEY": {
          "required": true
        },
        "JWT_SIGNING_PRIVATE_KEY": {
          "description": "A secret key for verifying the integrity of signed JWTs.",
          "generator": "secret",
          "required": true
        }
      },
      "postdeploy": "yarn heroku-review-app-init && yarn db:migrate && yarn db:seed"
    }
  }
}
