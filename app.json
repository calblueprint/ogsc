{
  "environments": {
    "review": {
      "addons": ["heroku-postgresql:hobby-dev"],
      "env": {
        "HEROKU_API_KEY": {
          "required": true
        },
        "JWT_SIGNING_PRIVATE_KEY": {
          "description": "A secret key for verifying the integrity of signed JWTs.",
          "generator": "secret",
          "required": true
        }
      }
    }
  },
  "scripts": {
    "release": "if [[ -z \"$HEROKU_APP_NAME\" ]]; then echo \"Skipping release script\"; else yarn heroku-review-app-init; fi"
  }
}
